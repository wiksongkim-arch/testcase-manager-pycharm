{
  "name": "testcase-manager",
  "version": "0.1.0",
  "description": "A comprehensive test case management system for software quality assurance",
  "private": true,
  "license": "Apache-2.0",
  "author": "TestCase Manager Team",
  "scripts": {
    "build": "node scripts/build.js",
    "build:all": "npm run build && npm run build:web && npm run build:api",
    "build:web": "cd apps/web && npm run build",
    "build:api": "cd services/api && npm run build",
    "dev": "npm run build && concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd services/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "test": "npm run build && cd packages/git-core && npm test"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jest": "^29.5.12",
    "@types/node": "^25.5.0",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ]
}